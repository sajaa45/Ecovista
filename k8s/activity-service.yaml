apiVersion: apps/v1
kind: Deployment
metadata:
  name: activity-service
  namespace: ecovista
spec:
  replicas: 2
  selector:
    matchLabels:
      app: activity-service
  template:
    metadata:
      labels:
        app: activity-service
    spec:
      containers:
      - name: activity-service
        image: ecovista-activity-service:latest
        imagePullPolicy: Never
        ports:
        - containerPort: 5001
        env:
        - name: MYSQL_HOST
          value: "mysql"
        - name: MYSQL_ROOT_PASSWORD
          valueFrom:
            secretKeyRef:
              name: mysql-secret
              key: MYSQL_ROOT_PASSWORD
        - name: SECRET_KEY
          valueFrom:
            secretKeyRef:
              name: mysql-secret
              key: SECRET_KEY
        readinessProbe:
          httpGet:
            path: /activity
            port: 5001
          initialDelaySeconds: 10
          periodSeconds: 5
        livenessProbe:
          httpGet:
            path: /activity
            port: 5001
          initialDelaySeconds: 30
          periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: activity-service
  namespace: ecovista
spec:
  selector:
    app: activity-service
  ports:
  - port: 5002
    targetPort: 5001
